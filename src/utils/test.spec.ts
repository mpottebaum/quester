import { describe, expect, it } from 'vitest'
import { testTestTest } from './test'

describe('testTestTest', () => {
  it('should should should', () => {
    const result = testTestTest()

    expect(result).toBe(null)
  })
})
